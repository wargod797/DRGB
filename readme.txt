This code Diffrentiate between red green blue blocks or objects from the image
if u want a live camera shot replace import image function a =imread('input.jpg'); with a=snapshot(cam);
as simple as that, 